# Placeholder directory

This directory is intentionally kept as a regular tracked folder (not a Git submodule).

A previous Git gitlink entry at this path caused `git submodule update --init --recursive`
to fail because there was no matching `.gitmodules` URL definition.
