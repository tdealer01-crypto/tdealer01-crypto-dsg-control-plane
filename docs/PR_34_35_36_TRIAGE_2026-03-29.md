# PR 34/35/36 triage decision (2026-03-29)

This file records the actionable decision for the read-side normalization PR chain.

## Decision

- **#34**: do not merge directly.
  - Reason: this is a large normalize/read-side fallback batch and carries risk of masking upstream inconsistency.
  - Action: recreate from `main` and cherry-pick only required hunks.

- **#35**: close.
  - Reason: superseded by **#39** (`fix(audit): avoid narrow determinism type predicate`).

- **#36**: do not merge directly.
  - Reason: logic is useful but the original PR was on a problematic branch and non-mergeable when checked.
  - Action: recreate from `main`, cherry-pick only the fallback behavior fix, then re-review.

## Runtime guard in this repo

`lib/dsg-core.ts#getDSGCoreDeterminism` keeps a **narrow fallback**:

- retry query-style endpoint only when path-style endpoint returns `404`
- no retry for non-404 errors (to avoid masking upstream failures)
