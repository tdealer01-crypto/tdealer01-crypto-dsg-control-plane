# Vercel redeploy trigger — 2026-05-18

Purpose: trigger a fresh production deployment after commit `600676fd11f6247886e88cc33ccc0402b154f841` was canceled before build logs were produced.

Expected included change:

- `app/github-app/page.tsx` manifest/display name changed from `DSG ONE Gate` to `DSG Gate v2`.

This file contains no secrets and has no runtime effect.
