import type { ReactNode } from 'react';

export const metadata = {
  title: 'OmniAgent Command Center | CEO THANAWAT',
  description: 'Unified Control Plane for AI orchestration, bounty hunting, and runtime governance',
};

export default function OmniAgentCommandCenterLayout({ children }: { children: ReactNode }) {
  return children;
}
