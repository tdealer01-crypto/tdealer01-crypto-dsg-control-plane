name: Test With Paths Filter

on:
  push:
    branches: [main]
    paths:
      - 'dsg-mcp-server/**'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Hello World"