name: Test No SSH Action

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "test"
      - name: Check secrets exist
        run: |
          if [ -z "${{ secrets.MCP_TARGET_HOST }}" ]; then
            echo "MCP_TARGET_HOST not set"
          else
            echo "MCP_TARGET_HOST is set"
          fi